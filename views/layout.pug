doctype html
html
  head
    title= title
    link(href="https://fonts.googleapis.com/icon?family=Material+Icons", rel="stylesheet")
    - var cssref = String(`compiled/${theme}.min.${version}.css`)
    link(
      id='pagestyle',
      rel='stylesheet',
      href=cssref)
    block head

  body
    block intro
    block content
    block bottomScripts